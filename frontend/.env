# The full URL to your backend API for the dev server proxy
VITE_API_PROXY_TARGET=http://localhost:8080

# The base path for API calls in the production build.
# If your API is at https://api.example.com, set this to https://api.example.com
# If it's served on the same host, you can leave this as /api
VITE_API_BASE_URL=/api
